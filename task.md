write a macos app. it is a gui to help running any docker compose files.

after runnning, it can add any docker compose file to it. The composer file can be edited by itself or outside editers afterwards.

from the added files, a user can pick which one to run

there is a pannel showing all runing composer files and their logs, keep 100,000 lines (it is a setting)


settings saved in a yaml file.


